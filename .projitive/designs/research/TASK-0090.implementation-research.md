# TASK-0090 Implementation Research Brief

- Task: Release Airx 0.9.0 - update CHANGELOG and version
- Status: IN_PROGRESS
- Created: 2026-05-07

## Summary

Airx 0.9.0 work is complete (ROADMAP-0004 closed) but not released. Tasks done: subpath exports (./browser, ./server), esbuild minification, enhanced error messages, benchmark suite. Need to: 1) Update CHANGELOG.md with 0.9.0 changes, 2) Bump version to 0.9.0 in package.json, 3) Create git tag v0.9.0

## Design Guidelines

- CHANGELOG.md follows Keep a Changelog format
- Sections: Added, Breaking Changes, Performance, Refactor, Fixed, Testing, Documentation, Infrastructure
- Most recent version goes at top
- Each entry has bold title + dash description

## Code Architecture Findings

### package.json (line 2)
- Current version: `"version": "0.8.0"`
- Exports already include `./browser` and `./server` subpaths (added in TASK-0085)
- These subpaths point to `output/render/browser/index.js` and `output/render/server/index.js`

### CHANGELOG.md
- Most recent entry: `[0.8.0] - 2026-05-03`
- All sections: Breaking Changes, Added, Performance, Refactor, Fixed, Testing, Documentation, Infrastructure

### Build output
- esbuild minification added (TASK-0084): build script now minifies with esbuild after TypeScript compilation
- Output files in `output/` directory are now minified

## 0.9.0 Changes to Document

### Performance
- **Bundle Minification** — Added esbuild post-build minification step, reducing bundle size ~40% (from ~50KB to <30KB raw ESM)
- **Subpath Exports** — Added `./browser` and `./server` subpath exports for tree-shaking (SSR users eliminate ~7.5KB hydrate code)
- **Benchmark Suite** — Added performance benchmark suite for 0.9.0 with historical comparison tracking

### Developer Experience
- **Enhanced Error Messages** — Improved error messages and debug experience for developers
- **Tree-shaking Audit** — Verified dead code elimination works correctly with subpath exports

### Documentation
- **Benchmark Documentation** — Improved benchmark docs with historical comparison and interpretation guidance

## Implementation Plan

1. Update CHANGELOG.md: Add `[0.9.0] - 2026-05-07` section at top with all 0.9.0 changes
2. Update package.json: Change `"version": "0.8.0"` to `"version": "0.9.0"`
3. Validate: Run `pnpm build && pnpm lint && pnpm test` to ensure nothing broke
4. Commit: `git add -A && git commit -m "release: bump to 0.9.0"`
5. Tag: `git tag v0.9.0 && git push && git push --tags`

## Validation Plan

- Run `pnpm build` - verify build succeeds with minified output
- Run `pnpm lint` - verify no lint errors
- Run `pnpm test` - verify all tests pass
- Verify `output/index.js` is minified (check file size < 30KB)
- Verify `./browser` and `./server` exports are in package.json

## Risks

- Low risk: this is a documentation/release task, no code changes
- Assumption: all 0.9.0 tasks were properly completed and tested before this release task was created
