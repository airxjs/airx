# TASK-0035 Implementation Research Brief

- Task: 提取 browser/server 共用 commit helper 函数
- Summary: 基于 TASK-0034 产出的研究简报，将 browser.ts 和 server.ts 中重复的 DOM 操作函数提取为共用模块。已完成 `getParentDom`、`getChildDoms` 的提取，验证无功能损失后重构两处调用。参考：source/render/browser/browser.ts:11-12、source/render/server/server.ts:11-12、source/render/basic/common.ts:137-168。
- Status: DONE
- Implementation Completed: 2026-03-31 (via commit 4a42c50)

---

## Design Guidelines and Specs

- `designs/optimization-roadmap-0.8.md` - Iteration B 阶段目标：提取 browser/server 共用 commit helper
- `designs/core/architecture.md` - 渲染层架构约束

## Code Architecture and Implementation Findings

### 已完成：getParentDom 和 getChildDoms 提取

**位置**：`source/render/basic/common.ts#L137-168`

**实现**：

```typescript
// common.ts:137-168
export function getParentDom<E extends AbstractElement>(instance: Instance<E>): E {
  if (instance.parent?.domRef != null) {
    return instance.parent.domRef
  }
  if (instance.parent) {
    return getParentDom(instance.parent)
  }
  throw new Error('Cant find dom')
}

export function getChildDoms<E extends AbstractElement>(instance: Instance<E>): E[] {
  const domList: E[] = []
  const todoList: Instance<E>[] = [instance]
  while (todoList.length > 0) {
    const current = todoList.pop()
    if (current?.domRef != null) {
      domList.push(current.domRef)
    }
    if (current?.domRef == null) {
      if (current?.child != null) {
        todoList.push(current.child)
      }
    }
    if (current?.sibling != null) {
      todoList.push(current.sibling)
    }
  }
  return domList
}
```

**browser.ts 引用**：`source/render/browser/browser.ts:11-12` (import) + `:98, :151` (usage)
**server.ts 引用**：`source/render/server/server.ts:11-12` (import) + `:246, :292` (usage)

### 验证：无本地重复实现

```bash
# 验证 browser.ts 无本地定义
grep -n "function getParentDom\|function getChildDoms" source/render/browser/browser.ts
# Exit code 1: 无匹配（已移除）

# 验证 server.ts 无本地定义
grep -n "function getParentDom\|function getChildDoms" source/render/server/server.ts
# Exit code 1: 无匹配（已移除）
```

**结论**：两处均从 `../basic/common.js` 导入，无本地重复实现。

---

## Implementation Plan

### ✅ 已完成

1. **提取 `getParentDom`**：`common.ts:137` 新增泛型函数，复用原有递归查找逻辑
2. **提取 `getChildDoms`**：`common.ts:157` 新增泛型函数，复用原有 BFS 遍历逻辑
3. **重构 browser.ts**：移除本地定义，改为从 `common.ts` 导入
4. **重构 server.ts**：移除本地定义，改为从 `common.ts` 导入
5. **验证无功能损失**：两处调用点不变，行为一致

### 后续任务（独立）

- **TASK-0034 相关**：提取 `updateDomProperties` 泛型版本（降低重复度）
- **后续任务**：设计 `commitInstanceDom` 的 adapter 接口

---

## Validation and Regression Test Plan

- [x] `getParentDom`、`getChildDoms` 在 browser 和 server 两端行为一致
- [x] 相关单元测试覆盖两个平台的调用路径（browser.test.ts, server.test.ts）
- [x] 编译无错误（tsc --noEmit 通过）
- [x] lint 无错误（npm run lint 通过）

---

## Risks and Open Questions

- **风险**：无（纯内部重构，无公开 API 变更）
- **未完成项**：`updateDomProperties` 和 `commitInstanceDom` 仍有重复，但不在本任务范围内

---

## Evidence

- Commit: `4a42c50 refactor(airx): extract getParentDom/getChildDoms to common.ts`
- browser.ts import: `../basic/common.js` (line 11-12)
- server.ts import: `../basic/common.js` (line 11-12)
