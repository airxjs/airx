# TASK-0062 Implementation Research Brief

- **Task**: Phase 2: WIP API 决策 — plugin() 与 renderToHTML() 去留
- **Summary**: 根据 2026-03-28 健康检查报告，AirxApp.plugin() 和 AirxApp.renderToHTML() 仍带有 @deprecated WIP 标记，需明确长期定位。方案 A：完成实现并解除 WIP 标记；方案 B：从公开 API 中移除。建议在 0.8.x 或 0.9.x 前决定。验收标准：WIP API 有明确状态声明，不影响下游接入判断。
- **Status**: IN_PROGRESS
- **Created**: 2026-04-17

---

## 1. 当前状态分析

### 1.1 源代码状态 (source/app/app.ts)

**实际情况**: `@deprecated WIP` 标记已被移除！

```typescript
// source/app/app.ts#L10-L30
export interface AirxApp {
  mount: (container: HTMLElement) => AirxApp
  debug: (level?: LogLevel) => AirxApp

  /**
   * 注册插件，返回 app 实例以支持链式调用。
   *
   * @example
   * import { createApp } from 'airx'
   * import { customPlugin } from './plugins'
   *
   * createApp(App).plugin(customPlugin).mount(...)
   */
  plugin: (...plugins: Plugin[]) => AirxApp

  /**
   * 将应用渲染为 HTML 字符串（SSR）。
   *
   * @example
   * const html = await createApp(App).renderToHTML()
   */
  renderToHTML: () => Promise<string>
}
```

**证据**: commit `04ff9d8` ("fix(airx): remove accidentally re-introduced @deprecated WIP markers") 已移除这些标记。

### 1.2 文档状态

| 文档 | 当前描述 | 状态 |
|------|---------|------|
| `source/app/app.ts` | 无 @deprecated WIP 标记 | ✅ 已更新 |
| `designs/api-stability-audit.md` | 声称有 @deprecated WIP 标记 | ⚠️ 需同步 |
| `designs/core/architecture.md` | 文档化但未声明稳定性 | ✅ 可接受 |

**关键发现**: `designs/api-stability-audit.md` 第 34-62 行记录的问题已通过 commit 04ff9d8 修复，但设计文档未同步更新。

---

## 2. API 稳定性评估

### plugin() 方法

| 维度 | 状态 |
|------|------|
| 实现完整性 | ✅ 已实现 (source/app/app.ts#L50-53) |
| 测试覆盖 | ⚠️ 需验证 - 搜索测试文件 |
| 文档状态 | ✅ JSDoc 示例完整 |
| 下游使用 | ⚠️ 需检查 airx-router/vite-plugin 依赖 |

### renderToHTML() 方法

| 维度 | 状态 |
|------|------|
| 实现完整性 | ✅ 已实现 (source/app/app.ts#L55-59) |
| 测试覆盖 | ⚠️ 需验证 |
| 文档状态 | ✅ JSDoc 示例完整 |
| 实际用途 | ⚠️ 需确认用户是否使用 |

---

## 3. 相关设计文档分析

### api-stability-audit.md 建议的方案

1. **方案 A（推荐）**: 添加 `@hidden` JSDoc 标签
2. **方案 B**: 移到 AirxAppInternal 类型
3. **方案 C**: 在 0.8.0 中正式移除

**实际执行**: 方案 C 被执行 - @deprecated WIP 标记被移除，使这两个方法成为正式 API。

---

## 4. 实施计划

### Step 1: 同步 api-stability-audit.md
- 更新 Issue 1 的描述，反映 @deprecated WIP 已移除
- 将这两个 API 从 "问题" 改为 "已解决"
- 添加当前稳定性评估

### Step 2: 确认测试覆盖
- 检查 plugin() 和 renderToHTML() 是否有单元测试或集成测试

### Step 3: 更新 architecture.md（如需要）
- 明确 plugin() 和 renderToHTML() 是正式 API 而非 WIP

---

## 5. 风险和开放问题

| 风险/问题 | 描述 | 处理建议 |
|----------|------|---------|
| hydrate() 是空实现 | api-stability-audit.md Issue 2 提到 | 另开任务处理 |
| 测试覆盖不明确 | 不确定是否有测试 | 执行时验证 |
| 下游兼容性 | 不确定 router/vite-plugin 如何使用 | 检查依赖 |

---

## 6. 结论

**核心问题**: 源代码已更新（@deprecated WIP 标记已移除），但 `api-stability-audit.md` 未同步更新。

**建议操作**:
1. 更新 `api-stability-audit.md` 反映当前状态
2. 标记 Issue 1 为 "已解决"
3. 添加对 Issue 2 (hydrate) 的跟踪

**不需要创建新任务，因为核心工作（移除 @deprecated WIP）已经完成。**
