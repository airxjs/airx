# TASK-0057 Implementation Research Brief

> **Task**: 修复 app.ts 意外重新引入的 @deprecated WIP 标记
> **Created**: 2026-04-16
> **Status**: DONE

## Summary

commit d99656e (2026-03-28) 添加 debug() API 时意外重新引入了 plugin()/renderToHTML() 的 @deprecated WIP 标记，覆盖了 bf2c6f9 中已完成的移除工作。

## Root Cause Analysis

1. **bf2c6f9 (2026-03-28 02:38)**: 正确移除了 `@deprecated WIP` 标记，为 `plugin()` 和 `renderToHTML()` 添加了完整的 JSDoc 示例
2. **d99656e (2026-03-28 16:20)**: 添加 `debug()` API 到 `AirxApp` 接口时，从旧版本代码复制粘贴了 `plugin/renderToHTML` 的接口定义（连同 `@deprecated WIP` 标记一起），意外覆盖了 bf2c6f9 的改动

## Evidence

| 检查项 | 状态 |
|--------|------|
| `git show bf2c6f9` diff 显示标记被移除 | ✅ |
| `git show d99656e` diff 显示标记被重新引入 | ✅ |
| 当前 HEAD `source/app/app.ts` 仍有 `@deprecated WIP` 标记 | ✅ 待修复 |
| bf2c6f9 后无其他 commit 明确移除这些标记 | ✅ |

## Implementation

**修复文件**: `source/app/app.ts`

**改动**:
- 移除 `plugin()` 和 `renderToHTML()` 接口定义上的 `/** @deprecated WIP */` 注释
- 恢复完整的 JSDoc 文档（与 bf2c6f9 一致）

**验证**:
- ✅ `npm run lint` 通过
- ✅ `npm run typecheck` 通过
- ✅ `npx vitest run source/app/app.test.ts` 15/15 测试通过
- ✅ `git push` 成功

## Commit

```
fix(airx): remove accidentally re-introduced @deprecated WIP markers

commit d99656e added debug() API but inadvertently copied @deprecated WIP
markers onto plugin()/renderToHTML(), regressing the fix from bf2c6f9.

Restore proper JSDoc with examples for both APIs.

Refs: TASK-0046, TASK-0057, ROADMAP-0001
```

## Risks

- **低风险**: 纯文档/JSDoc 修复，不影响任何运行时行为
- **无功能变更**: 仅恢复已验证的 JSDoc 文档

*Refs: TASK-0046, TASK-0057, ROADMAP-0001*
