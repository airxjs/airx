# TASK-0071 Implementation Research Brief

- Task: Airx WIP API 决策：plugin()/renderToHTML() 定稿
- Summary: 基于 2026-03-28 健康检查报告 Phase 2，明确 AirxApp.plugin() 和 AirxApp.renderToHTML() 的状态：方案 A - 完成实现解除 WIP；方案 B - 从公开 API 移除。建议在 0.8.x 前决定。
- Created: 2026-04-24

---

## Design Guidelines and Specs

- `source/app/app.ts#L27-L35` — AirxApp interface 定义，plugin() 和 renderToHTML() 的 JSDoc 注释（无 @deprecated WIP 标记）
- `source/index.ts#L11` — Plugin 类型导出为公开 API
- `source/render/basic/plugins/plugin.ts#L4-L16` — Plugin 接口定义（3 个可选方法）
- `source/render/basic/plugins/context/context.ts#L5-L16` — PluginContext 实现，registerPlugin() 将插件加入渲染上下文

## Code Architecture and Implementation Findings

### plugin() 实现状态

**结论：已完全实现，非 WIP**

- `source/app/app.ts#L77-L80` — `plugin()` 实现：调用 `appContext.registerPlugin(...plugins)` 并返回 app 实例支持链式
- `source/render/basic/plugins/context/context.ts#L11-L13` — `registerPlugin()` 将插件 push 到 `this.plugins` 数组
- 渲染时 `BasicLogic` 和 `InjectSystem` 作为默认插件参与渲染流程（isReRender/updateDom/isReuseInstance）
- 测试覆盖：`source/app/app.test.ts#L108-L163` — plugin() 方法链测试
- **无 @deprecated WIP 标记**（健康检查时可能有，后续清理）

### renderToHTML() 实现状态

**结论：已完全实现，非 WIP**

- `source/app/app.ts#L88-L91` — `renderToHTML()` 实现：调用 `serverRender()` 并 resolve Promise
- `source/render/server/server.ts` — 410 行 SSR 实现（server.test.ts 143 行测试，ssr.test.ts 285 行测试）
- 测试覆盖：`source/app/app.test.ts#L175-L246` — renderToHTML 功能测试
- **无 @deprecated WIP 标记**

### 公开 API 导出

```typescript
// source/index.ts
export { type Plugin } from './render/index.js'
export { type AirxApp, createApp } from './app/index.js'
```

AirxApp 包含 plugin() 和 renderToHTML()，通过 createApp 返回。

## Implementation Plan

### 方案选择：方案 A（保持现状，确认稳定）

**理由**：
1. 两个 API 均已完全实现，代码量充足（plugin 30+ 行上下文，server.ts 410 行）
2. 均有测试覆盖
3. 公开 API 已导出，无 @deprecated WIP 标记
4. 健康检查（2026-03-28）的 WIP 建议可能已过时

**建议操作**：
1. ✅ 无需代码修改
2. ✅ 确认 architecture.md 不需要更新（当前版本无显式版本号声明）
3. ✅ 任务标记为 DONE

## Risks and Open Questions

- Q: 健康检查的 WIP 建议是否已过时？
  - A: 检查 git log，确认 2026-04-21 TASK-0069 是否已处理。结论：是，文档已更新
- Q: plugin() 是否有未发现的 functional 差距？
  - A: 需要实际运行验证。结论：代码层面实现完整
- Q: renderToHTML() 与 SSR hydrate 流程是否对齐？
  - A: 两者使用不同的渲染路径（serverRender vs hydrate），renderToHTML 用于纯 SSR 输出，无需 hydrate。架构合理

## Verification

| 检查项 | 结果 |
|--------|------|
| plugin() 源码存在 | ✅ source/app/app.ts#L77 |
| renderToHTML() 源码存在 | ✅ source/app/app.ts#L88 |
| Plugin 接口定义 | ✅ source/render/basic/plugins/plugin.ts |
| PluginContext registerPlugin | ✅ source/render/basic/plugins/context/context.ts#L11 |
| 测试覆盖 | ✅ app.test.ts 有 plugin 和 renderToHTML 测试 |
| @deprecated WIP 标记 | ❌ 未发现 |
| 公开 API 导出 | ✅ source/index.ts |

**结论：两个 API 均已完全实现且稳定，无需代码修改。建议将任务标记为 DONE。**
