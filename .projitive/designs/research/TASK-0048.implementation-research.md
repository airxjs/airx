# TASK-0048 Implementation Research Brief

## Task
更新 architecture.md 反映 hydrate() 实现状态

## Summary
architecture.md 中 "Out of Scope" 提到 "Hydration (planned for 0.8.x)"，但 hydrate() 已在 TASK-0033 中实现（SSR hydration 接口 0.8.x）。应更新架构文档，将 Hydration 从 Out of Scope 移至 Scope，并更新 System Boundaries 和 Inputs/Outputs 表以反映 SSR + Hydration 能力。

## Changes Made

### 1. Scope 更新
- ✅ 从 "Out of Scope" 移除了 "Hydration (planned for 0.8.x)"
- ✅ 添加到 "Scope": "Hydration (client-side activation of SSR HTML)"

### 2. System Boundaries - Inputs 更新
- ✅ 新增 Input: "Pre-rendered HTML | SSR step | SSR output to be hydrated via `hydrate()`"

### 3. System Boundaries - Outputs 更新
- ✅ 更新 DOM mutations 描述: "Real DOM updates via `browserRender()` / `hydrate()`"

### 4. SSR Renderer 文档更新
- ✅ server.ts 描述从 "hydrate() — **Stub only** (planned for 0.8.x)" 改为 "hydrate() — Activates SSR-rendered HTML on the client for interactive updates"

### 5. Key Flows 新增 Hydration Flow
- ✅ 新增 "### Hydration Flow" 流程图，展示 hydrate() 如何将 SSR HTML 转化为响应式组件树

### 6. Change Triggers 更新
- ✅ 将 "Hydration implemented (0.8.x)" 标记为 ~~删除~~ ✅ Done in 0.7.x

## Verification
- typecheck: ✅ 通过
- lint: ✅ 通过
- tests: ✅ 319/319 通过
