# TASK-0049 Implementation Research Brief

Task: airx: hydrate() stub 添加运行时警告
Summary: 当前 hydrate() 是 no-op stub，用户调用后没有任何反馈。根据 0.8.x 路线图（roadmap-0.8-planning.md），hydrate 是 0.8.0 的核心目标，当前应添加运行时警告告知用户该功能未实现，避免误解。

## Design Guidelines and Specs

- `designs/roadmap-0.8-planning.md#L93-L97` — hydrate stateSnapshot restore is not yet implemented; placeholder
- `designs/optimization-roadmap-0.8.md#L58` — "TODO(0.8.x): hydrate signal state restoration is not yet implemented"
- `README.md` — hydrate documented as "Future: Activate SSR HTML on the client"

## Code Architecture and Implementation Findings

### hydrate() 实现位置
- `source/render/browser/hydrate.ts#L73` — 顶层 hydrate 函数实现
- `source/render/server/server.ts#L435` — SSRApp.hydrate 调用 clientHydrate
- `source/render/browser/hydrate.ts#L128` — restoreSignalStates 是 placeholder 实现

### 警告策略
- 只在 development 模式（NODE_ENV !== 'production'）输出警告，避免生产环境噪音
- 警告放在 restoreSignalStates 内部，只在用户传递 stateSnapshot 时触发
- 警告文本指向 0.8.x roadmap，不指向不存在的 issue

### 改动范围
- 只修改 `source/render/browser/hydrate.ts` 一个文件
- build: ✅ 通过 (tsc)
- test: ✅ 319 tests passed

## Alternatives Considered

1. **无条件 console.warn** — ❌ 过于干扰，每次调用都弹警告
2. **只改 README 标注 Future** — ❌ 代码层面没有警示，开发者可能在生产环境使用
3. **throw Error** — ❌ 过于激进，hydrate 核心功能是可用的

## Execution Summary

- 改动: 11 行新增，5 行删除
- 验证: build ✅ test ✅ 319/319
- 提交: `b6bc70e fix(airx): warn in dev mode when hydrate stateSnapshot restore is incomplete`
