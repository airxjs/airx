# TASK-0076 Implementation Research Brief

## Task
Review airx README and identify next actionable milestone

---

## Summary

All 75 previous tasks are DONE. Based on README and roadmap review, the next logical milestone is **Airx 0.8.0 Stable Release** with two focused work items:
1. Finalize and stabilize the `hydrate()` API (remove experimental tag)
2. Decide fate of `plugin()` / `renderToHTML()` WIP APIs

---

## Design Guidelines and Specs

- `.projitive/designs/roadmap-0.8-planning.md` - 0.8.x version planning document
- `.projitive/designs/api-stability-audit.md` - API stability audit findings
- `.projitive/roadmap.md` - All 3 roadmaps (ROADMAP-0001/0002/0003) are DONE
- `CHANGELOG.md` - Version history
- `source/render/server/server.ts#L322-L411` - Current hydrate implementation

---

## Code Architecture and Implementation Findings

### Current State (v0.7.10)

**hydrate() - Status: Functional but Experimental**
- File: `source/render/server/server.ts#L363-369` - SSRApp.hydrate() method
- File: `source/render/server/server.ts#L397-405` - Top-level hydrate() function
- File: `source/render/browser/index.ts` - clientHydrate implementation
- Current: Delegates to client-side hydrate, works but marked @experimental
- Warning message points to CHANGELOG.md#080

**plugin() / renderToHTML() - Status: WIP Deprecated**
- Both marked `@deprecated WIP` in JSDoc
- According to TASK-0062/TASK-0068: both are now stable but still have WIP markers
- Need to either: (A) remove WIP markers officially, or (B) remove from public API

### Key Files
- `source/index.ts` - Public API exports
- `source/render/server/server.ts` - SSR and hydrate implementation
- `source/render/browser/index.ts` - Browser renderer (contains clientHydrate)

---

## Implementation Plan

### Proposed Milestone: Airx 0.8.0

**TASK-NEW-1: Finalize hydrate() API for stable release**
- Remove @experimental marker from hydrate() JSDoc
- Update README to reflect hydration is no longer experimental
- Update CHANGELOG to reflect stable hydration in 0.8.0
- Validation: npm run typecheck && npm test passes

**TASK-NEW-2: Resolve plugin()/renderToHTML() WIP status**
- Based on TASK-0062 findings: both APIs work correctly
- Decision needed: Remove WIP markers (keep APIs) OR remove from public API
- Recommended: Keep APIs, remove WIP markers, add @stable tag
- Update all affected documentation

**TASK-NEW-3: Version bump to 0.8.0**
- Update package.json version to 0.8.0
- Update CHANGELOG with 0.8.0 release notes
- Ensure all tests pass before release

---

## Risks and Open Questions

1. **Risk**: Removing experimental marker commits to API stability - but hydrate has been working since 0.7.x
2. **Open**: Should we do a beta release first (0.8.0-beta.1) before stable?
3. **Open**: Does `plugin()` / `renderToHTML()` have active users? Need to check npm downloads or GitHub stars

---

## Verification

- [ ] npm run typecheck passes
- [ ] npm test passes (all 378+ tests)
- [ ] npm run lint passes
- [ ] Build succeeds: npm run build
- [ ] README examples work correctly