# TASK-0025 Implementation Research Brief

- **Task**: airx WIP API (plugin/renderToHTML) 决策与代码清理
- **Summary**: 分析 AirxApp.plugin() 和 AirxApp.renderToHTML() 的实际状态，决定处置方案
- **Status**: DONE

---

## Design Guidelines and Specs

- `source/app/app.ts` - App interface definition
- `source/render/index.ts` - Plugin/PluginContext exports

## Code Architecture and Implementation Findings

### plugin()

**当前状态**: ✅ **功能完整，只是标记过时**

- 位置: `source/app/app.ts#L56-60`
- 实现: 调用 `appContext.registerPlugin(...plugins)` 并返回 `this` 支持链式调用
- 测试: 12 tests in `app.test.ts` (包括 plugin chaining, multiple plugins)
- 结论: **无需废弃，移除 @deprecated WIP 标记**

### renderToHTML()

**当前状态**: ✅ **功能完整，只是标记过时**

- 位置: `source/app/app.ts#L67-72`
- 实现: 调用 `serverRender()` 返回 Promise\<string\>
- 测试: `app.test.ts` 有完整测试
- 结论: **无需废弃，移除 @deprecated WIP 标记**

## Decision Made

**方案：保留 API，移除 @deprecated WIP 标记，补充 JSDoc 文档**

理由：
1. 两个 API 都功能完整，测试覆盖充分
2. plugin() 是扩展机制的核心入口，有实际价值
3. renderToHTML() 是 SSR 能力的一部分
4. @deprecated WIP 标记是状态遗留，不表示实际质量问题

## Implementation Plan

1. ✅ 移除 `source/app/app.ts` 中的两个 `@deprecated WIP` JSDoc 注释
2. ✅ 为 `plugin()` 补充 JSDoc 示例
3. ✅ 为 `renderToHTML()` 补充 JSDoc 示例
4. ✅ Build: PASS
5. ✅ Lint: PASS
6. ✅ Tests: 183/183 PASS

## Risks and Open Questions

- 无重大风险。这是清理过时标记，不改变行为。
- 如果未来要废弃这两个 API，可以重新标记。

*Refs: TASK-0025, ROADMAP-0001*
